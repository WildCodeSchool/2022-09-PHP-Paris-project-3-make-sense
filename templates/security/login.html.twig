{% extends 'base.html.twig' %}

{% block title %}Make_Sense . Connexion{% endblock %}

{% block body %}

    <div class="container">
        <h1 class= "mt-5 text-center">Connexion</h1>
        <h6 class= "mt-3 text-center">Se connecter pour continuer</h6>

        {% if error %}
        <div class="alert alert-primary" role="alert">
            A simple primary alert—check it out!
        </div>
        {% endif %}

        <div class="row ">
            <form class="mt-5" action="{{ path ('security.login') }}" method="POST">
                <div class="mt-5 offset-4 col-4">
                    <label for="username" class="form-label">Adresse email</label>
                    <input type="email" class="form-control" id="username" name="_username" placeholder="exemple@makesense.fr" value="{{ last_username}}">
                </div>
                <div class=" mt-5 offset-4 col-4">
                    <label for="Password" class="form-label">Mot de passe</label>
                    <input type="password" class="form-control" id="Password" name="_password" placeholder="********">
                </div>
                <button type="submit" class="btn btn-danger mt-5 offset-4 col-4">Se connecter</button>
            </form>
        </div>
    </div>

{% endblock %}
