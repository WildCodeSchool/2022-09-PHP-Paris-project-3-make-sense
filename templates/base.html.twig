<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>

		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body>
		{% block header %}{% endblock %}
		{% block body %}{% endblock %}
	</body>
</html>

  {# $request = " SELECT department.name FROM department JOIN 
decision_department ON department_id = department.id JOIN decision ON decision.id = decision_id"; #}