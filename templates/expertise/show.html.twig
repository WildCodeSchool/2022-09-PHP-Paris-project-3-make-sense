{% extends 'base.html.twig' %}

{% block title %}Expertise{% endblock %}

{% block body %}
    <h1>Expertise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expertise.id }}</td>
            </tr>
            <tr>
                <th>IsExpert</th>
                <td>{{ expertise.isExpert ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Expert Email</th>
                <td>{{ expertise.user.email }}</td>
            </tr>
            <tr>    
                <th>department</th>
                <td>{{ expertise.department.name }}</td>
            </tr>    
        </tbody>
    </table>

    <br>
    <a href="{{ path('app_expertise_index') }}">back to list</a>

    <a href="{{ path('app_expertise_edit', {'id': expertise.id}) }}">edit</a>

    {{ include('expertise/_delete_form.html.twig') }}
{% endblock %}
