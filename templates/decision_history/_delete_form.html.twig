<form method="post" action="{{ path('app_decision_history_delete', {'id': decision_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decision_history.id) }}">
    <button class="btn">Delete</button>
</form>
