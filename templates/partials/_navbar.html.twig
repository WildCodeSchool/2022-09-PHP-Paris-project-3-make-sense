<nav class="navbar navbar-expand-lg navbar-light border border-dark">
	<div class="container-fluid">
		<div class="nav nav-pills nav-fill col-4">
			<a class="dropdown-item" href="{{ path('app_dashboard') }}">
				<img class="logo" src="{{ asset('build/images/logo-makesense.png') }}">
			</a>
		</div>
		<div class="col-8 nav justify-content-end align-items-center">
			    {% if app.request.attributes.get('_route') == 'app_dashboard' %}
						<li class="nav-item  col-3">
							<div class="row offset-1 form ">
								{{ form_start(form, {'attr': {'id': 'form_search'}}) }}
								<div>
									{{ form_widget(form.search_title, {'attr': {'class': 'form-control'}}) }}
								</div>
								<div class="">
									<button class=" btn text-black my-2 my-sm-0"><i class="fa fa-search"></i></button>
								</div>
								{{ form_end(form) }}
							</div>
						</li>
				{% endif %}
			<li class="nav-item offset-1">
				<a href="{{ path('app_decision_search') }}" class="fs-4 me-5 btn" type="button">
					Décisions
				</a>
			</li>
			<a href="{{ path('app_notification') }}" class="fs-4 btn" type="button">
				Notification
			</a>
			{{ render(controller('App\\Controller\\NotificationController::sumNotification', {default: 'Loading...'})) }}
		</li>
		{# <li class="nav-item">
			<div class="dropdown">
				<a href="#" class="fs-4 me-5 btn dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
					Mon Profil
				</a>

				{{ render(controller('App\\Controller\\NotificationController::sumNotification', {default: 'Loading...'})) }}</li>
		</li> #}
		<li class="nav-item">
			<div class="dropdown">
				<a href="#" class="fs-4 me-5 btn dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
					Mon Profil
				</a>
				<ul class="dropdown-menu">
					<li>
						<a class="fs-5 dropdown-item" href="#">Mes décisions</a>
					</li>
					<li>
						<a class="fs-5 dropdown-item" href="#">Se déconnecter</a>
					</li>
				</ul>
			</div>
		</li>
	</div>

</div></nav>
